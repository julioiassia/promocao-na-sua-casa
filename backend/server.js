const express=require('express');const multer=require('multer');const crypto=require('crypto');const Anthropic=require('@anthropic-ai/sdk');const path=require('path');const {createClient}=require('@supabase/supabase-js');const app=express();app.use(express.json());app.use(express.static(path.join(__dirname,'..')));app.use((req,res,next)=>{res.header('Access-Control-Allow-Origin','*');res.header('Access-Control-Allow-Headers','*');next();});const upload=multer({storage:multer.memoryStorage(),limits:{fileSize:30*1024*1024}});const supabase=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_KEY);const hashes=new Set();async function pdfParaImagens(buffer){const {pdf}=require('pdf-to-img');const doc=await pdf(buffer,{scale:2});const paginas=[];for await(const pagina of doc){paginas.push(pagina);}return paginas;}function parseJSONSeguro(texto){const t=texto.trim().replace(/```json|```/g,'').trim();try{return JSON.parse(t);}catch(e){try{const i=t.indexOf('{');const f=t.lastIndexOf('}');if(i!==-1&&f!==-1)return JSON.parse(t.substring(i,f+1));}catch(e2){}return{produtos:[]};}}async function extrairComIA(anthropic,conteudo,tipo){const prompt='Extraia TODOS os produtos com precos deste encarte de supermercado. Retorne APENAS JSON valido: {"produtos":[{"nome":"nome completo com quantidade","marca":"marca","preco":0.00,"unidade":"un","confianca":"alta"}]}. Se nao encontrar: {"produtos":[]}';let messages;if(tipo==='imagem'){messages=[{role:'user',content:[{type:'image',source:{type:'base64',media_type:conteudo.mimeType,data:conteudo.data}},{type:'text',text:prompt}]}];}else if(tipo==='paginas'){const content=conteudo.map(p=>({type:'image',source:{type:'base64',media_type:'image/png',data:p.toString('base64')}}));content.push({type:'text',text:prompt});messages=[{role:'user',content}];}const resp=await anthropic.messages.create({model:'claude-haiku-4-5-20251001',max_tokens:8000,messages});return parseJSONSeguro(resp.content[0].text);}async function salvarPromocoes(produtos,mercadoNome,hash){const inserts=produtos.map(p=>({mercado_nome:mercadoNome,nome:p.nome,marca:p.marca||'',preco:p.preco,unidade:p.unidade||'un',foto_hash:hash}));const{error}=await supabase.from('promocoes').insert(inserts);if(error)throw new Error('Erro ao salvar: '+error.message);return inserts;}app.post('/upload-admin',upload.single('foto'),async(req,res)=>{try{if(!req.file)return res.status(400).json({erro:'Sem foto'});const hash=crypto.createHash('sha256').update(req.file.buffer).digest('hex');if(hashes.has(hash))return res.status(400).json({erro:'Foto ja enviada'});const anthropic=new Anthropic({apiKey:process.env.ANTHROPIC_API_KEY});const dados=await extrairComIA(anthropic,{mimeType:req.file.mimetype,data:req.file.buffer.toString('base64')},'imagem');if(!dados.produtos||!dados.produtos.length)return res.status(422).json({erro:'Nenhum produto encontrado.'});hashes.add(hash);res.json({sucesso:true,produtos:dados.produtos});}catch(e){console.error(e);res.status(500).json({erro:e.message});}});app.post('/upload-pdf-admin',upload.single('pdf'),async(req,res)=>{try{if(!req.file)return res.status(400).json({erro:'Sem PDF'});const hash=crypto.createHash('sha256').update(req.file.buffer).digest('hex');if(hashes.has(hash))return res.status(400).json({erro:'PDF ja enviado'});const paginas=await pdfParaImagens(req.file.buffer);if(!paginas.length)return res.status(422).json({erro:'Nao foi possivel processar o PDF.'});const anthropic=new Anthropic({apiKey:process.env.ANTHROPIC_API_KEY});const todosProdutos=[];for(let i=0;i<Math.min(paginas.length,5);i++){const dados=await extrairComIA(anthropic,[paginas[i]],'paginas');if(dados.produtos&&dados.produtos.length)todosProdutos.push(...dados.produtos);}if(!todosProdutos.length)return res.status(422).json({erro:'Nenhum produto encontrado no PDF.'});hashes.add(hash);res.json({sucesso:true,produtos:todosProdutos,paginas:paginas.length});}catch(e){console.error(e);res.status(500).json({erro:e.message});}});app.post('/publicar',async(req,res)=>{try{const{produtos,mercado}=req.body;if(!produtos||!produtos.length)return res.status(400).json({erro:'Sem produtos'});const salvos=await salvarPromocoes(produtos,mercado,'manual-'+Date.now());res.json({sucesso:true,total:salvos.length});}catch(e){console.error(e);res.status(500).json({erro:e.message});}});app.get('/promocoes',async(req,res)=>{try{const{data,error}=await supabase.from('promocoes').select('*').order('criado_em',{ascending:false});if(error)throw error;res.json(data);}catch(e){res.status(500).json({erro:e.message});}});app.get('/mercados',async(req,res)=>{try{const{data,error}=await supabase.from('mercados').select('*');if(error)throw error;res.json(data);}catch(e){res.status(500).json({erro:e.message});}});const PORT=process.env.PORT||3001;app.listen(PORT,()=>console.log('APP PROMOCAO NA SUA CASA rodando na porta '+PORT));